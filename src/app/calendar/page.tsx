import { Layout } from "@/components/Layout";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Calendar - AI Student Calendar",
  description: "Your smart student calendar with AI-powered suggestions for optimal scheduling and life balance.",
};

export default function CalendarPage() {
  return <Layout />;
}
